# 🚀 بدء سريع - تشغيل الخادم

## ⚠️ المشكلة الحالية
الخطأ `ERR_CONNECTION_REFUSED` يعني أن **الخادم غير قيد التشغيل**.

## ✅ الحل في 3 خطوات

### 1️⃣ افتح PowerShell
- اضغط `Win + X`
- اختر "Windows PowerShell" أو "Terminal"

### 2️⃣ انتقل إلى مجلد backend
```powershell
cd "d:\New folder (2)\backend"
```

### 3️⃣ شغّل الخادم
```powershell
npm start
```

## ✅ يجب أن ترى:
```
✔ Server running at http://localhost:3000
✔ Uploads directory: d:\New folder (2)\backend\uploads
✔ Database file: d:\New folder (2)\backend\database.db
```

## ⚠️ مهم جداً:
- **لا تغلق نافذة PowerShell** - الخادم يعمل فيها
- **اتركها مفتوحة** طوال الوقت الذي تستخدم فيه الموقع

## 🧪 اختبار سريع

بعد أن ترى الرسالة أعلاه، افتح متصفح جديد واكتب:
```
http://localhost:3000/api/admin/products
```

إذا ظهرت بيانات JSON، فمعنى ذلك أن كل شيء يعمل! ✅

## ❌ إذا لم يعمل:

### الخطأ: "npm: command not found"
**الحل**: تثبيت Node.js من [nodejs.org](https://nodejs.org)

### الخطأ: "Cannot find module"
**الحل**:
```powershell
cd "d:\New folder (2)\backend"
npm install
```

### الخطأ: "Port 3000 is already in use"
**الحل**: أغلق البرامج الأخرى أو غيّر PORT في `.env`

---

**بعد تشغيل الخادم، يمكنك استخدام الموقع بشكل طبيعي!** 🎉
