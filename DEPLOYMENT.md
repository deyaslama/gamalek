# دليل النشر على Render.com

## خطوات النشر

### 1. إعداد المستودع (Repository)

✅ تأكد من رفع جميع الملفات إلى GitHub/GitLab/Bitbucket
✅ تأكد من وجود ملف `render.yaml` في الجذر
✅ تأكد من وجود ملف `.gitignore` لحماية الملفات الحساسة

### 2. إنشاء خدمة على Render

1. سجل الدخول إلى [Render.com](https://dashboard.render.com)
2. اضغط على "New +" ثم اختر "Web Service"
3. اربط مستودعك (GitHub/GitLab/Bitbucket)
4. اختر المستودع والمشروع
5. Render سيكتشف تلقائياً ملف `render.yaml` ويستخدم الإعدادات منه

### 3. إعداد متغيرات البيئة

في صفحة الخدمة، اذهب إلى "Environment" وأضف:

```
EMAIL_USER = your-email@gmail.com
EMAIL_PASS = your-gmail-app-password
```

**ملاحظة**: `PORT` و `NODE_ENV` موجودان في `render.yaml` ولا حاجة لإضافتهم يدوياً.

### 4. إعداد Gmail App Password

1. افتح [حساب Google](https://myaccount.google.com/)
2. اضغط على "الأمان" (Security)
3. فعّل "التحقق بخطوتين" (2-Step Verification) إن لم يكن مفعلاً
4. اذهب إلى "كلمات مرور التطبيقات" (App Passwords)
5. اختر "البريد" و "جهاز آخر"
6. انسخ كلمة المرور المولدة
7. الصقها في `EMAIL_PASS` على Render

### 5. إعداد النطاق المخصص

1. في صفحة الخدمة، اذهب إلى "Settings"
2. اضغط على "Custom Domains"
3. أضف النطاق: `gamalek.store`
4. أضف النطاق الفرعي: `www.gamalek.store`
5. Render سيعطيك تعليمات DNS

### 6. إعداد DNS

اذهب إلى مزود DNS الخاص بك (مثل Cloudflare، Namecheap، إلخ) وأضف:

#### الخيار 1: CNAME (موصى به)
```
Type: CNAME
Name: www
Value: gamalek-store.onrender.com
```

```
Type: CNAME  
Name: @
Value: gamalek-store.onrender.com
```

#### الخيار 2: A Record
```
Type: A
Name: @
Value: [Render IP Address - سيظهر في لوحة التحكم]
```

**ملاحظة**: قد يستغرق انتشار DNS من 5 دقائق إلى 48 ساعة.

### 7. التحقق من النشر

1. بعد النشر، افتح `https://gamalek.store`
2. تأكد من تحميل الصفحة الرئيسية
3. جرب تسجيل الدخول
4. تأكد من عمل API endpoints

### 8. استكشاف الأخطاء

#### المشكلة: الخدمة لا تبدأ
- تحقق من Logs في Render Dashboard
- تأكد من صحة متغيرات البيئة
- تأكد من تثبيت جميع التبعيات

#### المشكلة: CSS/JS لا يعمل
- تحقق من مسارات الملفات في HTML
- تأكد من أن `express.static` يخدم الملفات بشكل صحيح

#### المشكلة: قاعدة البيانات فارغة
- هذا طبيعي! قاعدة البيانات SQLite محلية وستبدأ فارغة
- يمكنك إضافة بيانات من لوحة التحكم

#### المشكلة: البريد الإلكتروني لا يعمل
- تحقق من `EMAIL_USER` و `EMAIL_PASS`
- تأكد من استخدام App Password وليس كلمة المرور العادية
- تحقق من تفعيل "التحقق بخطوتين" في Gmail

### 9. تحديثات مستقبلية

عند رفع تغييرات جديدة إلى المستودع:
- Render سيكتشف التغييرات تلقائياً
- سيعيد بناء ونشر الخدمة تلقائياً
- قد تستغرق العملية 2-5 دقائق

### 10. نسخ احتياطي

⚠️ **مهم**: قاعدة البيانات SQLite محلية وستفقد البيانات عند:
- إعادة النشر
- إعادة تشغيل الخدمة
- حذف الخدمة

**الحلول المقترحة**:
- استخدام PostgreSQL (متوفر على Render)
- استخدام قاعدة بيانات خارجية
- عمل نسخ احتياطي دوري

## الدعم

إذا واجهت أي مشاكل:
1. تحقق من Logs في Render Dashboard
2. راجع ملف `README.md` للتفاصيل
3. تحقق من إعدادات DNS
4. تأكد من صحة متغيرات البيئة

---

**تم النشر بنجاح! 🎉**

