# إعدادات Render.com - دليل مفصل

## 📋 الخطوة 1: إنشاء الخدمة (Web Service)

### عند إنشاء خدمة جديدة:

1. **Name (اسم الخدمة)**: `gamalek-store`
2. **Environment**: اختر `Node`
3. **Region**: اختر الأقرب لك (مثلاً: `Frankfurt` أو `Oregon`)
4. **Branch**: `main` أو `master` (حسب فرعك الرئيسي)
5. **Root Directory**: اتركه فارغاً (سيستخدم الجذر تلقائياً)
6. **Build Command**: `cd backend && npm install`
7. **Start Command**: `cd backend && npm start`

> **ملاحظة**: إذا كان لديك ملف `render.yaml`، Render سيكتشفه تلقائياً ويستخدم الإعدادات منه.

---

## ⚙️ الخطوة 2: إعدادات Environment Variables

اذهب إلى **Settings** → **Environment** وأضف المتغيرات التالية:

### المتغيرات المطلوبة:

| Key | Value | ملاحظات |
|-----|-------|---------|
| `EMAIL_USER` | `your-email@gmail.com` | بريدك الإلكتروني الكامل |
| `EMAIL_PASS` | `xxxx xxxx xxxx xxxx` | Gmail App Password (16 حرف بمسافات) |
| `NODE_ENV` | `production` | (اختياري - موجود في render.yaml) |
| `PORT` | `10000` | (اختياري - Render يضبطه تلقائياً) |

### كيفية الحصول على Gmail App Password:

1. اذهب إلى [Google Account](https://myaccount.google.com/)
2. اضغط على **الأمان** (Security)
3. فعّل **التحقق بخطوتين** (2-Step Verification) إن لم يكن مفعلاً
4. ابحث عن **كلمات مرور التطبيقات** (App Passwords)
5. اختر:
   - **التطبيق**: البريد
   - **الجهاز**: جهاز آخر (أدخل: Render)
6. انسخ الكلمة المكونة من 16 حرف (مثل: `abcd efgh ijkl mnop`)
7. الصقها في `EMAIL_PASS` على Render

---

## 🌐 الخطوة 3: إعدادات Custom Domain

اذهب إلى **Settings** → **Custom Domains**

### أضف النطاقات التالية:

1. **Domain 1**:
   - `gamalek.store`
   - Type: `Apex Domain`

2. **Domain 2**:
   - `www.gamalek.store`
   - Type: `Subdomain`

### بعد إضافة النطاقات:

Render سيعطيك تعليمات DNS. ستحتاج إلى:

#### للنطاق الرئيسي (gamalek.store):

**الخيار 1 - CNAME (موصى به)**:
```
Type: CNAME
Name: @
Value: gamalek-store.onrender.com
TTL: 3600
```

**الخيار 2 - A Record**:
```
Type: A
Name: @
Value: [Render IP Address - سيظهر في لوحة التحكم]
TTL: 3600
```

#### للنطاق الفرعي (www.gamalek.store):

```
Type: CNAME
Name: www
Value: gamalek-store.onrender.com
TTL: 3600
```

> **ملاحظة**: Render سيظهر لك القيم الصحيحة في لوحة التحكم بعد إضافة النطاق.

---

## 🔧 الخطوة 4: إعدادات Build & Deploy

### في قسم **Build & Deploy**:

1. **Auto-Deploy**: ✅ **Yes** (للنشر التلقائي عند الـ Push)
2. **Pull Request Previews**: ✅ **Yes** (اختياري - لمعاينة PRs)
3. **Branch**: `main` أو `master`

### Build Settings (إذا لم تستخدم render.yaml):

- **Build Command**: `cd backend && npm install`
- **Start Command**: `cd backend && npm start`

---

## 💾 الخطوة 5: إعدادات Disk (اختياري)

⚠️ **مهم**: قاعدة البيانات SQLite وملفات الرفع ستُفقد عند إعادة النشر.

### إذا أردت تخزين دائم:

1. اذهب إلى **Settings** → **Disk**
2. اضغط **Add Disk**
3. **Name**: `data-disk`
4. **Size**: `1 GB` (أو حسب احتياجك)
5. **Mount Path**: `/opt/render/project/src/backend`

> **تحذير**: هذا سيكلفك رسوم إضافية. للحلول المجانية، استخدم قاعدة بيانات خارجية.

---

## 🔒 الخطوة 6: إعدادات Security

### HTTPS:

✅ **Auto TLS**: مفعّل تلقائياً - لا حاجة لإعداد

### Headers (اختياري):

يمكنك إضافة Security Headers في `server.js`:

```javascript
app.use((req, res, next) => {
  res.setHeader('X-Content-Type-Options', 'nosniff');
  res.setHeader('X-Frame-Options', 'DENY');
  res.setHeader('X-XSS-Protection', '1; mode=block');
  next();
});
```

---

## 📊 الخطوة 7: إعدادات Monitoring (اختياري)

### Health Checks:

- **Health Check Path**: `/` (موجود في render.yaml)
- Render سيفحص هذا المسار كل 30 ثانية

### Logs:

- يمكنك رؤية Logs في الوقت الفعلي من **Logs** tab
- Logs محفوظة لمدة 7 أيام في الخطة المجانية

---

## 💰 الخطوة 8: إعدادات Plan

### الخطة المجانية (Free):

- ✅ **Auto-Sleep**: بعد 15 دقيقة من عدم الاستخدام
- ✅ **750 ساعة/شهر** مجاناً
- ❌ لا يوجد تخزين دائم
- ❌ لا يوجد نسخ احتياطي تلقائي

### الخطة المدفوعة (Starter - $7/شهر):

- ✅ **Always On** - لا ينام
- ✅ **512 MB RAM**
- ✅ **0.5 CPU**
- ✅ **Disk Storage** متاح

---

## ✅ الخطوة 9: التحقق من الإعدادات

### قائمة التحقق:

- [ ] تم إضافة `EMAIL_USER` و `EMAIL_PASS`
- [ ] تم إضافة النطاق `gamalek.store`
- [ ] تم إضافة النطاق `www.gamalek.store`
- [ ] تم إعداد DNS في مزود النطاق
- [ ] تم تفعيل Auto-Deploy
- [ ] تم التحقق من Logs - لا توجد أخطاء

---

## 🧪 اختبار الإعدادات

### بعد النشر:

1. افتح `https://gamalek.store`
2. تحقق من تحميل الصفحة الرئيسية
3. جرب API: `https://gamalek.store/api/admin/products`
4. تحقق من Logs في Render Dashboard

### إذا واجهت مشاكل:

1. **الخدمة لا تبدأ**:
   - تحقق من Logs
   - تأكد من صحة `EMAIL_USER` و `EMAIL_PASS`
   - تأكد من تثبيت التبعيات (`npm install`)

2. **النطاق لا يعمل**:
   - تحقق من إعدادات DNS (قد تستغرق 24-48 ساعة)
   - تأكد من إضافة النطاق في Render
   - استخدم [DNS Checker](https://dnschecker.org/) للتحقق

3. **CSS/JS لا يعمل**:
   - تحقق من مسارات الملفات
   - تأكد من أن `express.static` يخدم الملفات

---

## 📝 ملخص الإعدادات الأساسية

```
Name: gamalek-store
Environment: Node
Build Command: cd backend && npm install
Start Command: cd backend && npm start

Environment Variables:
  - EMAIL_USER: your-email@gmail.com
  - EMAIL_PASS: your-app-password
  - NODE_ENV: production

Custom Domains:
  - gamalek.store
  - www.gamalek.store
```

---

**تم الإعداد بنجاح! 🎉**

